menu: {
  id: "randomtp"
  # Will default to "menus.[id]"
  permission: "menus.randomtp"
  # We should support mini message here. TODO
  title: ""
  # Available Types:
  # Normal: CHEST (1-6), WORKBENCH, HOPPER, DISPENSER, BREWING
  # Scrolling (1-6)
  # Paginated (1-6)
  # Storage (1-6)
  type: CHEST
  rows: 4 # only good for chest/scrolling/paginated/storage
  items: [
    { # Id doesn't matter, it's just for the end user.
      row: 1
      column: 2
      display-name: "" # We can have an empty line here, and we will grab the displayname from ia instead.
      material: "PAPER" #hdb:, itemsadder:namespace:id
      amount: 1
      # duplicate: "1:1-3,2:1-4" #row:index-index optional
      # custom-model-data: 0 optional
      on-left-click: [
        #"command:",
        #"command:",
        #"open-link:",
        "message:"
      ]
      on-shift-click: [

      ]
      on-right-click: [

      ]
    },
    {

    }
  ]
  fillers: {
    style: solid #solid, alternating, two-two, #TODO
    filler: {

    },
    filler: {

    }
  }
  # Preconfigured actions we can use TODO
  default-action: {

  }
  open-action: {

  }
  close-action: {

  }
}
version: 1